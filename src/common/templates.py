from __future__ import annotations

"""Default prompt templates.

These defaults are used when the value is not configured in:
1) DB settings table (admin panel), and
2) .env (environment variables).

Placeholders supported:
- {original_text} – original caption text from the source post (may be empty).

Notes:
- Templates are plain text. For Telegram captions we generate HTML (Telegram-supported subset).
- The admin panel supports multiline values (send them as one message).
"""


DEFAULT_KIE_REGEN_TEMPLATE = """Сделай новый уникальный кадр на основе референс-изображения.

Сохрани настроение, палитру, стиль/жанр, качество и общий «вайб» референса, но измени композицию и детали так, чтобы результат выглядел как другой кадр (не копия).

Требования:
- Один главный объект или сюжетный центр.
- Без текста, логотипов, водяных знаков.
- Высокая детализация, чистая картинка, без артефактов.

Доп. контекст (может помочь, но не обязателен):
{original_text}
"""


DEFAULT_REWRITE_TEMPLATE = """✦

<Спокойный живой вход — 1 строка>

<Что это за кадр — 1 строка>
<Почему он выглядит по-настоящему — 1 строка>

Если откликается — попробуй
👇
"""
